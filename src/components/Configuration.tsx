const user = {
  name: 'Shigeru',
  isLoggedIn: false,
};

const game = {
  difficulty: 5,
  maxTryCount: 10,
}

const screen = {
  minfontsize: 10,
  fontsizecoeff: 0.05,
};

export default {user, game, screen};